# Copy to .env.local (gitignored) and fill in. run.ps1 loads these into the process env.
# Only needed if you use the alternate executor / 2nd reviewer: --executor gemini or --dual-review.
GEMINI_API_KEY=

# Optional overrides (otherwise auto-detected per project):
# ORCHESTRA_STACK=android-compose
# ORCHESTRA_DESIGN_GLOBS=**/ui/theme/*.kt,**/tokens.json
# ORCHESTRA_VERIFY_CMD=./gradlew assembleDebug
